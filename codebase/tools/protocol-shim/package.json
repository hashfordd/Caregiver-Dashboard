{
  "name": "@alzcare/protocol-shim",
  "version": "0.0.0",
  "private": true,
  "description": "Translates the hardware prototype's patient/{id}/raw/* MQTT messages into the canonical device/{uuid}/{telemetry,signals,events} contract the bridge ingests.",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@alzcare/shared": "*",
    "@supabase/supabase-js": "2.45.4",
    "mqtt": "5.10.1"
  },
  "devDependencies": {
    "@types/node": "20.17.0",
    "tsx": "4.16.5",
    "typescript": "5.6.3"
  }
}
