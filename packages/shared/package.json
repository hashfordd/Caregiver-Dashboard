{
  "name": "@alzcare/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared Zod schemas + inferred TS types — single source of truth for MQTT message contracts and DB row shapes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./mqtt": "./src/mqtt/index.ts",
    "./mqtt/*": "./src/mqtt/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"(shared has no tests yet)\" && exit 0",
    "build": "echo \"(shared is consumed as TS source; no build step)\" && exit 0"
  },
  "dependencies": {
    "zod": "3.23.8"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
